import React from 'react'
import { washingMachineUrls, ibhubsLogo, profile } from '.'

/*global jest*/
/*global expect*/
describe('test assets for assets', () => {
   it('should test data in assets', () => {
      expect(washingMachineUrls).toBeDefined
   })
})
