import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const LanguageChangeWrapper = styled.div`
   ${tw`flex`} >button {
      padding: 2px 4px;
      margin: 2px 4px;
   }
`

const TeluguButton = styled.button``

const EnglishButton = styled.button``

const HindiButton = styled.button``

export { LanguageChangeWrapper, TeluguButton, EnglishButton, HindiButton }
