import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const ActiveAndInactiveContainer = styled.div`
   ${tw`flex justify-center `}
`

const ActiveAndInactiveBar = styled.div`
   ${tw``}
`

export { ActiveAndInactiveContainer, ActiveAndInactiveBar }
