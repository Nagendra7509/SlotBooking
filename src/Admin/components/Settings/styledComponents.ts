import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const SettingsPageContainer = styled.div``

const SideNavBarAndSettings = styled.div`
   ${tw`flex`}
`

const SettingsHeading = styled.h1``

export { SettingsPageContainer, SideNavBarAndSettings, SettingsHeading }
